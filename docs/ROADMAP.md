# Bounce — Production MVP Roadmap

Path from the current working demo to a production-ready, **multi-tenant** SaaS that can take real customers and pay real operators.

_Last updated: 2026-07-13._

## Where we are

The core product loop works end-to-end and is deployed (https://bounce-app.vercel.app):
conversational AI quote → deposit/full checkout (Stripe) → e-signed booking (SignWell)
→ operator dashboard / calendar / inbox, all on live data.

**Solid today**
- Booking + availability engine (date ranges, peak-reservation math, oversell detection, abandoned-checkout expiry).
- Stripe Checkout with idempotent webhooks; SignWell agreement send path built + verified (currently **off in prod** — test mode; see Go-Live Checklist).
- Conversational AI quote agent grounded in live inventory + DB prices.
- Operator app + storefront, wired to real data, responsive, deployed.

**Known gaps (verified in code)**
- **No authentication** — middleware only refreshes Supabase cookies; operator app is open to anyone. `getDefaultOperator()` (16 call sites) hardcodes one operator.
- **Single Stripe account, no Connect** — all payments land in one account.
- **No transactional email** — no receipts/confirmations/operator alerts.
- **In-memory rate limiting** — per-instance, ineffective across serverless.
- ~~No automated tests / CI~~ ✅ **Vitest suite (79 tests) + GitHub Actions CI** (lint/typecheck/test on every push/PR); ~~shared dev↔prod DB~~ ✅ split. _Remaining gap: migrations are CLI-tracked but still hand-applied per deploy._

---

## 🚀 Go-Live Checklist (your actions, in order)

The single copy-paste list of everything that must be flipped/entered in the real accounts before taking real customers. Each item links to its detailed entry deeper in the roadmap. **All env changes are in Vercel → Production** unless noted. Nothing here is code — the code is built and verified; these are account/config actions only.

**1. Database — Supabase Pro on prod**
- [ ] Upgrade the prod project (`urmqfxsajoboibjqhtmn`, "Rentals App") to the **Pro plan** — the free tier auto-pauses on inactivity and has no backups. _(See Go-live hardening, Tier 0.)_

**2. Stripe — go live**
- [ ] Swap Vercel Production to **live keys**: `STRIPE_SECRET_KEY=sk_live_…` + `STRIPE_WEBHOOK_SECRET=whsec_…` from a **new live webhook endpoint** → `https://<app>/api/webhooks/stripe`, subscribed to `checkout.session.completed` + the 5 subscription events.
- [ ] **Enable Connect in live mode** (operators re-onboard — test connected accounts don't carry over).
- [ ] **Create live billing prices** (re-run `scripts/setup_billing.mjs` against the live account) so plan `lookup_key`s resolve.
- [ ] Verify a real charge + refund + a subscription signup end-to-end. _(See Stripe — go live, Tier 0.)_

**3. SignWell — turn contracts on** _(all four must be set together, or the `autoSendEnabled()` gate keeps agreements off — this is the current prod state)_
- [ ] **`SIGNWELL_AUTO_SEND=true`** — ⚠️ **currently empty in prod → auto-send is OFF.** This is the master switch; agreements do not send until it's `true`.
- [ ] **`SIGNWELL_TEMPLATE_ID`** — the **real** rental-agreement template (today's is the `basic-rental-agreement` placeholder). _(Confirm it has a value.)_
- [ ] **`SIGNWELL_SENDER_EMAIL`** — the "from" address for signing requests. _(Confirm it has a value.)_
- [ ] **`SIGNWELL_TEST_MODE=false`** — flips from free/non-binding **test** docs to **billable + legally binding** live docs. Only do this once the real template is in place.
- [ ] Confirm the **live webhook** (`SIGNWELL_WEBHOOK_ID`) targets the prod endpoint.
- [ ] After the template edits: flip `SIGNWELL_SINGLE_SIGNER=true` + `SIGNWELL_TEMPLATE_FIELDS=true` (single-signer + policy-embed items). _(See SignWell — go live, Tier 0.)_
- _Note: B2 (operators upload their own contract) stays **blocked** until a template-capable SignWell tier — not required to go live._

**4. SMS — Twilio + A2P 10DLC** _(optional at launch; the Text channel stays hidden until done)_
- [ ] Twilio account + number; **A2P 10DLC** brand/campaign registration (days-long carrier approval); set `TWILIO_ACCOUNT_SID` / `TWILIO_AUTH_TOKEN` / `TWILIO_FROM`; point the inbound webhook at `/api/webhooks/twilio`; apply migration `0022`. _(See SMS external setup, Inquiries Phase 2.)_

**5. Email — Resend domain**
- [ ] Set `RESEND_FROM` to a **verified domain** (default `onboarding@resend.dev` only delivers to the account owner). _(See Transactional email, Tier 0.)_

**6. Legal**
- [ ] Fill the real values in `src/lib/legal/company.ts` (entity, jurisdiction, contact, address, effective date).
- [ ] **Counsel review** of the Terms + Privacy drafts **and** the rental agreement's soundness. _(See Legal/trust, Tier 0.)_

**7. Error monitoring — Sentry** _(recommended; code is deployed + dormant)_
- [ ] Create a Sentry project, then set **`NEXT_PUBLIC_SENTRY_DSN`** in Vercel Production (turns on runtime error capture). Optional for readable stack traces: `SENTRY_AUTH_TOKEN` + `SENTRY_ORG` + `SENTRY_PROJECT`. _(See Error monitoring, Tier 2.)_

---

## Tier 0 — Production blockers

- [x] **Auth + operator accounts + plan billing** — Supabase Auth login + self-serve sign-up (creates an isolated tenant); `operator_members` mapping; middleware gates operator routes; `getSessionOperator()` in the operator app. Plan selection (Free / Solo $29 / Growing $59) with **Stripe subscription billing + 14-day trial**. _Follow-ups: ✅ **billing webhook** — the Stripe webhook now syncs the full subscription lifecycle onto the operator (matched by `stripe_customer_id`): `customer.subscription.created/updated/deleted` + `invoice.payment_failed/succeeded` → `subscription_status` (trialing→active→past_due→canceled) and `plan`. ✅ **plan capability matrix + limits** (`plan.ts` `PLAN_CAPABILITIES`: Free = **5 catalog items / 20 AI quotes-mo / no team**; Solo/Growing unlimited). **Lapse policy = downgrade-to-Free, grandfathering** existing items (catalog cap enforced on item-**create**, so a downgraded operator keeps their items + storefront but can't grow past the cap); **`past_due` = grace** (keeps the paid plan while Stripe retries; only a terminal end drops to Free). `effectivePlanId()` is the read-side belt to the webhook's suspenders. Verified all transitions + the cap read against the live schema. **⚠ Setup: add these 5 event types to the Stripe webhook endpoint** (`customer.subscription.created/updated/deleted`, `invoice.payment_failed`, `invoice.payment_succeeded`) — else Stripe won't deliver them. Remaining: ✅ **AI-quote monthly counter** (done — migration `0038`; see the Tier-0 item below); dunning email on `payment_failed`, email verification, Stripe Customer Portal._
- [~] **Multi-tenancy** — ✅ session-scoped operator resolution; ✅ **per-operator storefront routing** (`slug` → `/s/[slug]`, scoped catalog/quote/booking/payouts, verified isolated; `/book` redirects to the default operator). _Remaining: RLS policies keyed to membership (defense-in-depth); subdomains / custom domains. ✅ **per-operator API keys for bring-your-own-frontend** — see "Operator API & embeddable storefront" in Tier 3._
- [x] **Stripe Connect** — Express connected accounts; onboarding via `/api/connect/onboard` → hosted Account Link → `/connect/return` records `charges_enabled`; customer checkout uses **destination charges** (`transfer_data.destination`) to route funds to the operator (platform fee via `PLATFORM_FEE_BPS`, default 0 since the subscription is the revenue). "Set up payments" banner on the dashboard until connected. _Prereqs: enable Connect in the Stripe dashboard (one-time). Follow-ups: `account.updated` webhook for post-onboarding status changes; consider direct charges if operators should bear Stripe processing fees._
- [x] **Transactional email** (Resend) — `src/lib/email/` (REST, no SDK; best-effort, skips cleanly if `RESEND_API_KEY` unset). Wired: booking confirmation/receipt to the customer + "new booking" alert to the operator (paid webhook); operator "new inquiry" alert (needs-review); inquiry **reply delivered to the customer** (completes the inbox Send-reply). _Setup: add `RESEND_API_KEY` (+ `RESEND_FROM` with a verified domain — default `onboarding@resend.dev` only delivers to the Resend account owner). Follow-ups: balance reminders; email-verification gating on signup._
- [~] **Go-live hardening** — live Stripe + webhook, live SignWell keys, **live Twilio + A2P 10DLC** for SMS (see the SMS external-setup item under Inquiries Phase 2). ✅ **Prod DB split — DONE.** Dev + prod no longer share a database. **Adopted the Supabase CLI** (`supabase/config.toml`) so migrations are reproducible; a **new project (`urmqfxsajoboibjqhtmn`, "Rentals App")** is **production**, built clean by `supabase db push` applying all 43 migrations in order (verified: every table/bucket/RPC present, zero rows). Vercel **Production** env (`NEXT_PUBLIC_SUPABASE_URL` / anon / service-role — the app reads `NEXT_PUBLIC_SUPABASE_URL`, not `SUPABASE_URL`) now points at it; **`.env.local` + Preview/Dev stay on the old project (`vjgurdppmwxdlhczswdb`)**, which keeps the demo tenants for local testing. Prod started **clean slate** — the real operator (Bounce USA) was created via a fresh signup on production; **no demo tenants in prod**. Auth URL config set on the new project (Site URL + redirect allow-list); signup auto-confirms via the admin API so confirm-email can stay on. _Remaining: **live Stripe + SignWell keys** (detailed sub-items below); live **Twilio + A2P 10DLC** (SMS external-setup item under Inquiries Phase 2); **Pro plan** on the prod project before real traffic (free tier auto-pauses on inactivity + no backups). ✅ **dev migration tracking done** — both projects CLI-tracked (dev back-filled via `supabase migration repair`); `./scripts/db-migrate.sh` applies new migrations to prod + dev. Follow-up: add Supabase env vars to **Preview** if PR preview deploys are used (currently Production-only)._
  - [ ] **Stripe — go live (your action):** in Vercel **Production**, swap to live keys — `STRIPE_SECRET_KEY=sk_live_…` + `STRIPE_WEBHOOK_SECRET=whsec_…` from a **new live webhook endpoint** (Stripe dashboard in **live** mode → Developers → Webhooks) pointed at `https://<app>/api/webhooks/stripe`, subscribed to `checkout.session.completed` **and** the 5 subscription events (`customer.subscription.created/updated/deleted`, `invoice.payment_failed`, `invoice.payment_succeeded`). Then: **enable Connect in live mode** (operators re-onboard via live Account Links — test connected accounts don't carry over); **create the live billing prices** so the plan `lookup_key`s resolve (re-run `scripts/setup_billing.mjs` against the live account). App reads `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` / `PLATFORM_FEE_BPS` only — checkout is **hosted/redirect**, so there's no browser publishable key. Verify a real charge + refund + a subscription signup end-to-end. _Note: env vars live in Vercel, not the DB — the split didn't touch them; but the live webhook must point at the prod URL._
  - [~] **SignWell — go live (your action):** ✅ **SignWell API plan active** (pay-as-you-go, 25 free docs/mo). ✅ **auto-send verified working** — creating a document from the default template returns 201 (`sendAgreementForOrder` functional). **Currently kept in TEST MODE** (`SIGNWELL_TEST_MODE` defaults to test; not set to `false`) — test documents are free + not legally binding, fine while setting up. **⚠️ Also confirmed 2026-07-13: `SIGNWELL_AUTO_SEND` is empty in Vercel Production → auto-send is OFF** (correct while in test mode — you don't want non-binding test contracts on real bookings; turn it on as part of the go-live cutover). _To go live for real bookings: set **`SIGNWELL_AUTO_SEND=true`** + **`SIGNWELL_TEST_MODE=false`** in Vercel Production (live docs are **billable + legally binding**); confirm the **live webhook** (`SIGNWELL_WEBHOOK_ID`) targets the prod endpoint; land the **real rental-agreement template** as `SIGNWELL_TEMPLATE_ID` (today's is the `basic-rental-agreement` placeholder) + do the **single-signer + policy-field** template edits, then flip `SIGNWELL_SINGLE_SIGNER=true` + `SIGNWELL_TEMPLATE_FIELDS=true`._ Note: `SIGNWELL_API_APPLICATION_ID` is in Production but the **B2 embedded editor is blocked** on this plan (template creation 401s — see the B2 note under "Per-operator contract identity" in Tier 1).
- [x] **Durable rate limiting** — moved the in-memory, per-instance limiter to **Upstash Redis** (sliding-window, shared across all serverless instances) via the Vercel Marketplace integration (`KV_REST_API_URL/TOKEN`). Same call shape, now async (`await checkRateLimit`); the six public endpoints (inquiries, inquiries/contact, quote, bookings, checkout, signup) updated. **Graceful fallback** to the in-memory limiter when the vars are absent (local dev) or a Redis call fails — degrades to soft/per-instance rather than erroring the request. `Ratelimit` instances cached per (limit, window); namespaced keys keep endpoints separate. Verified live against the provisioned store (round-trip + a 3/60s window enforcing exactly). _Setup: Upstash store provisioned + connected to Production/Preview/Development; free tier ~10k commands/day covers current traffic (over-cap falls back to in-memory rather than failing)._
- [x] **Free-tier AI-quote monthly counter (enforce the 20/mo cap)** — durable per-operator monthly counter (migration `0038`: `operator_ai_usage` table + atomic `increment_ai_usage` RPC, keyed by UTC calendar month so it resets with no cron). Enforced in the single AI choke point `handleInquiry`: gates a **new** conversation (no `inquiryId`) **before any model call**, so a capped operator never spends against our Anthropic bill; in-progress threads continue; counts **once per conversation** (on first inquiry persist), not per turn; paid plans are unlimited and skip the DB. When capped it still **captures the lead** (a needs-review inquiry, no AI draft/quote) + alerts the operator, and the customer sees a graceful "leave your email" message (never billing language). Surfaced in **Settings → account** ("AI quotes: N / 20 this month") with a working **Upgrade** CTA — `billing/checkout` now accepts a target plan so a Free operator can move to Solo. Verified atomic increment under 10 concurrent calls + the cap math against the live schema. _Setup: migration `0038` applied to the shared DB._
- [~] **Legal/trust** — ✅ **Terms of Service** (`/terms`) + **Privacy Policy** (`/privacy`) shipped as reviewable drafts (static-prerendered, app-styled `LegalShell`), plus a **minimal cookie notice** (essential-cookies-only, dismissal remembered in localStorage, mounted globally). Placeholders for entity/jurisdiction/contact/effective-date are centralized in `src/lib/legal/company.ts` — fill once, both docs update. Linked from **signup** (acceptance line under the button), the **storefront sidebar**, and the cookie notice. The Privacy Policy names our processors (Stripe, SignWell, Resend, Twilio, Supabase, Vercel, Upstash, Anthropic, US Census geocoder) and the controller/processor split (we're processor for Renter data). _Remaining (both required before relying on the pages, plus rental-agreement soundness — pairs with the SignWell single-signer / policy-embed items):_
  - [ ] **Fill the placeholders (your action)** — set the real values in `src/lib/legal/company.ts`: legal entity name (`company`), governing-law state/jurisdiction (`governingLaw`), legal/privacy contact email (`contactEmail`), mailing address (`mailingAddress`), and effective date (`effectiveDate`). It's the single source of truth — changing it there updates both `/terms` and `/privacy` everywhere. No code change needed beyond the values.
  - [ ] **Counsel review (your action)** — have a qualified lawyer review the Terms + Privacy drafts before you rely on them (they're general-purpose templates, not legal advice, and the pages say so on-screen). Confirm they fit your entity, jurisdiction, plan/billing terms, and data practices; incorporate any edits. Also confirm the **rental agreement** in SignWell is legally sound.

## Tier 1 — Operator product completeness

- [~] **User management — multi-user accounts + roles** — an operator account can have multiple users with **Admin / Employee** roles (migrations `0044` role remap owner/staff→admin/employee, `0045` `operator_invites`). **Shipped:** RBAC via `getSessionMembership()` → `{operator, role}` + `requireAdmin()` guard on every admin-only action/route (settings, billing, connect, API keys, documents, promos, inventory CRUD, refund/cancel); employees redirected from admin pages + admin nav hidden; **aggregate financials hidden** from employees (dashboard revenue, Month insights, CRM Collected) while per-booking totals/balances stay. **Money line:** employees CAN take money **in** (collect balance card + cash) but not **out** (refund/cancel = admin). **Team UI** (Settings → Team, admin-only, **Growing-plan-gated**): invite by email+role, change role, remove — with a **last-admin guard** (can't demote/remove the only admin) and **auth-account deletion** on removal when the user is on no other team. **Invite flow** (`/invite/[token]`): invitee sets name + password (or accepts as themselves if already signed in as the invited email; a *different* signed-in user is told to sign out — no accidental consumption). **Identity:** greeting + sidebar show the signed-in user's own name/role (stored in `user_metadata`), not the operator's owner; **/account** self-service for name / email / password (all roles). Verified end-to-end (invite→accept→role→guards→removal→login) + shipped to prod. _Remaining/later: RLS on the new tables; multi-operator switcher (a user on several teams currently resolves to their earliest); per-page audit of any remaining employee-visible $ if you tighten further; the mobile `/more` page is still a placeholder._


- [x] **Inventory management UI** — `/inventory` lists the operator's catalog; add/edit/delete items (name, category, price, rate, quantity, description, visibility, power) via a drawer + server actions (operator-scoped). (Item photos: separate item below.)
- [x] **Inventory item photos (image upload)** — operators upload **multiple photos per item** in the inventory drawer (client-side resize → `POST /api/inventory/photo` uploads via the service role to a public `item-photos` bucket at `{operatorId}/{uuid}`), **choose a primary** (first) + remove; stored in the existing `items.images` column. Rendered on the **storefront item cards + operator inventory list** (icon fallback when none). Orphaned files cleaned up on edit-remove + item delete. Verified end-to-end (bucket → upload → public URL → card render) via the service role, since the upload UI itself is behind operator login. _Setup: migration `0017` creates the bucket (already applied to the shared DB during verification). Follow-ups: photo lightbox/carousel (own item below); `next/image` optimization (remotePatterns) instead of plain `<img>`; sweep for photos uploaded-then-abandoned before save._
- [x] **Photo lightbox / carousel** — clicking a product photo (storefront item cards, the chat quote card, and the checkout "Review & book" summary) opens a shared `Lightbox` (`LightboxProvider` context in the storefront shell) — a carousel of **all** of that item's photos with prev/next arrows, touch swipe, keyboard nav (←/→, Esc), clickable dots (active tracks the slide), scrim + close-button dismissal, body-scroll lock, and `role="dialog"`/`aria-modal`. Off-screen slides load on demand. Card thumbnails show a **+N** badge when there's more than one photo. Verified end-to-end on the public storefront (open → carousel nav → dots → close + scroll-lock restore). _Left out: the operator inventory list (its thumbnail opens the edit drawer). Follow-ups: full focus-trap tab-cycling; preload adjacent slides._
- [x] **Settings** — `/settings` (was stub): editable **business profile** (name, owner, phone, geocoded service area, **timezone** — drives "today" for calendar/route/dashboard, migration `0019`), **booking policies** (deposit %, auto-quote cap, min lead time — wired into checkout, storefront, and the AI escalation gate), **flexible sales tax** (rental-taxable vs. delivery-taxable split, migration `0026`), **delivery pricing** (flat / by-zone / by-distance via the US Census geocoder — operator picks, migration `0027`), **availability** (operating days, delivery windows, blackout ranges — wired into checkout + calendar, migration `0031`), **customer policies** (cancellation + damage text, migration `0028`), **notification preferences** (migration `0029`), **branding** (accent color / logo / tagline / about, migrations `0030` + `0032`), and a **storefront/account** panel (storefront URL, plan/subscription, AI-quote usage, Connect status + connect). Hardened `/api/items` to expose only public operator fields.
- [x] **Booking management** — `/bookings/[id]` detail page (operator-scoped) with actions: mark delivered / completed, cancel (frees inventory), and **refund** (Stripe refund → order refunded + booking canceled). Wired from the calendar's "Open booking". _Follow-up: edit a booking's items/dates._
- [x] **Operator-created quote / booking** — a **Booking builder** drawer (catalog item picker with live availability + dates + customer fields + a live price preview) launchable from the **Dashboard**, the **Calendar** (pre-fills the selected day), and an **Inquiry** ("Create quote", pre-filled from the request — the key path for over-cap escalations). Two commit modes the operator chooses: **Send payment link** — emails the customer a public **`/pay/[bookingId]`** page that runs the existing Stripe checkout (pay → reserve + confirm + contract via the webhook); **Record as booked** — reserves atomically (`reserve_booking`) + confirms immediately for cash/phone orders, with an optional recorded deposit. Launched from an inquiry it sets `inquiry.booking_id`, so the inquiry's outcome flips to Booked/Pending. Verified the pay page render + `/api/checkout` (real Stripe session + atomic reserve) end-to-end. No migration. **Unified with the escalated-inquiry AI suggestion:** the AI-drafted-quote card has a **Review & send quote** CTA that opens the builder **pre-filled** with the suggested items/customer/dates + an editable **note to the customer** (the AI's drafted message, sent atop the quote email) — so the operator sends as-is or modifies. The plain-text composer is now a secondary follow-up channel (no longer pre-filled with the AI draft).
- [x] **Driver daily route interface (deliveries)** — `/deliveries` is now a **mobile-first daily route** (operator-only, behind the existing login). Drop-offs (bookings **starting** the day) + pick-ups (bookings **ending** the day) from committed bookings (`paid`→`completed`), ordered by delivery window; single-day rentals appear in both. Each stop shows a type badge, time window, customer, items, and address with one-tap **Open in Maps**, **Call/Text** (customer phone now captured at checkout → `customer_phone`, migration `0018`) or an Email fallback, a Booking link, and **Mark delivered / picked up** (reuses `markDeliveredAction` / `markCompletedAction` → advances status, revalidates calendar + dashboard + deliveries). Day switcher (prev / next / Today via `?d=`). Route data verified against live bookings. _Setup: run migration `0018` (adds `customer_phone`) — **required before deploy**, since booking-create + the route query both reference it. Follow-ups: **driver access** (shareable per-day link or a driver role); proof-of-delivery (photo/signature) + per-stop notes; geographic route optimization, map view, multi-driver assignment._
- [x] **Wire the Inquiries inbox actions** — "Send reply" records the operator's (edited) reply + marks the inquiry `replied` (clears it from "needs you"); "Dismiss" → `dismissed` (hidden). Operator-scoped server actions; dashboard/badge counts update. Customer delivery via email (Resend) is wired. _Follow-up: persistent conversation, see next item._
- [~] **Inquiries → persistent conversation thread** — **Phase 1 (outbound) done.** An `inquiry_messages` table (migration `0021`) is now the conversation thread — senders `customer` / `operator` / `ai`, backfilled from the existing `inbound_message`, auto `ai_summary`, and `operator_reply`. The inbox detail renders the **full chronological thread** and the **composer is always available on every inquiry regardless of status** — each send appends to the thread, emails the customer (Resend), and updates status; the AI draft is now a *suggestion* that pre-fills the composer for `needs_review` (no longer the only way to reply). This kills the one-shot dead-end. `createInquiry` seeds the thread (customer message + AI auto-answer), `replyToInquiry` appends an operator message (and still sets `operator_reply` for the list preview). _Setup: run migration `0021` — **required before deploy** (inbox load, inquiry creation, and replies all use the table)._ **Phase 2 (inbound two-way) remains — its own item below.**
- [~] **Inquiries Phase 2 — inbound two-way messaging** — the email thread is still outbound-only; the **SMS path is now built (full AI loop)**. Remaining: the inbound-**email** side + the SMS **bootstrap UI** (below).
  - [x] **SMS two-way (full AI loop, shared-number)** — a Twilio SMS provider (`src/lib/sms/`, `fetch`-based, no SDK; graceful no-op without `TWILIO_*` env, like email/esign) + an inbound webhook `POST /api/webhooks/twilio`. Flow: verify the Twilio signature → route the text to the customer's inquiry by phone (`findLatestInquiryByPhone`, shared-number model, migration `0022` adds `inquiries.customer_phone`) → append it to the `inquiry_messages` thread as `customer` → run the **same `handleInquiry` brain as the web chat** over the reconstructed thread → reply over SMS (TwiML `<Message>`); a `review` outcome flips the inquiry to `needs_review` + alerts the operator, a `quoted` outcome appends a storefront reserve link. Idempotent by `MessageSid` (shared `webhook_events`). Operator replies from the inbox now deliver over SMS when the inquiry is a text thread (channel-aware `replyInquiryAction`). Verified: Twilio signature vs. the real SDK (`getExpectedTwilioSignature` / `validateRequest`), the thread→Anthropic message normalization (starts-with-user, alternating), build. ✅ **Bootstrap UI** — the inquiry composer now has an **Email / Text** channel toggle (shown only when Twilio is configured, via `smsEnabled` from the page). Picking **Text** reveals a phone field (pre-filled from the inquiry) and the **Text** button calls `sendInquirySmsAction` → sets `customer_phone` + `channel='sms'`, appends the message to the thread, and texts the customer — which establishes the phone↔inquiry mapping so their reply routes back through the loop. Migration `0022` **required before deploy** (createInquiry/reply/bootstrap touch `customer_phone`). _Remaining (SMS): **cold inbound** (customer texts first, unprompted → needs per-operator numbers), and an **operator-takeover** rule so the AI pauses while a human is actively handling a thread._
  - [ ] **SMS external setup (go-live — your action, required to send/receive)** — the code degrades to a logged no-op and the Text toggle stays hidden until this is done: (1) a **Twilio account** + a phone number (or Messaging Service); (2) **A2P 10DLC** brand + campaign registration for that number — a US carrier requirement submitted via Twilio (days-long approval, small fees); unregistered application-to-person SMS is filtered/blocked; (3) set in Vercel: `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM` (E.164), and optional `TWILIO_WEBHOOK_URL` (the exact public webhook URL, for signature verification behind proxies); (4) in the number's Messaging config, point the inbound webhook at `POST https://<app>/api/webhooks/twilio`; (5) apply migration `0022`. Cost note: Twilio charges per number/month + per message.
  - [ ] **Inbound email side** — capture email replies into the thread too: a plus-addressed reply-to (`inbox+<inquiryId>@<domain>`) + a Resend inbound webhook (verify, strip quoted chains, match the inquiry, append a `customer` message). Reopens the inquiry + fires the operator "new reply" notification. Considerations: sender verification, spam/auto-reply-loop guards, attachments.
- [x] **Inquiry loop — contact capture + honest delivery** — closed the outbound gap. The storefront chat never collected an email, so an escalated ("needs you") inquiry had no reply-to and hitting **Send** in the inbox silently delivered nothing (the seed inquiries have emails, which masked it). Now: when the AI **escalates** (status `review`), the chat shows an inline **"leave your email and we'll send your custom quote"** capture → `POST /api/inquiries/contact` attaches it to the inquiry (operator-scoped, rate-limited). And the inbox composer is **honest about delivery** — "Emails {address}" when contact is on file, or a **"No email — can't deliver"** warning with **Send disabled** when it isn't. Verified the capture endpoint + operator scoping; the assistant already emits `review` on escalation. Also fixed a contradiction: an escalated quote is **not self-bookable** (the operator sends a custom price), so the assistant now returns an escalation-appropriate customer message and the storefront **hides the bookable quote card** on `review` — showing the custom-quote message + email capture instead of a bookable quote alongside it. _(Customers replying back into the thread is still Inquiries Phase 2.)_
- [x] **Inquiry → booking outcome indicator** — the inbox now shows whether each inquiry **converted** (especially useful on auto-answered ones, which the operator never saw): a **Booked / Checkout-started / Canceled / No-booking-yet** banner at the top of the detail (with amount, event date, and a link to the booking) + a compact badge on the list card. Linked two ways: **explicit** — booking from the chat quote writes `inquiry.booking_id` (threaded chat → checkout → `/api/bookings` → `linkInquiryToBooking`); **heuristic** — otherwise matched by customer email + overlapping dates (batched into two booking queries, not N+1). Pairs with the persistent thread: spot a non-converted auto-response and follow up. Both paths verified; no migration (the `inquiries.booking_id` column already existed).
- [x] **Balance collection** — booking detail shows the balance due with **Collect balance** (Stripe checkout for total − deposit; webhook marks it paid-in-full without re-confirming/re-sending) and **Mark paid (cash)**. Charges only the remaining amount; doesn't reset the committed booking.
- [ ] **SignWell contract → single signer (customer-only)** — the `basic-rental-agreement` template has two signer roles, so both the company (`SIGNWELL_SENDER_EMAIL`) and the customer get a signing email. Decision made: **customer-only signing**. Needs (1) template edit so "Document Sender" has no signature field, then (2) drop the company signer in `sendAgreementForOrder` (`src/lib/esign/agreements.ts`); optionally gate a countersigner behind an env flag.
- [~] **Customer policies → embedded in the contract** — **code shipped & dormant** (commit `dbac09b`): `sendAgreementForOrder` merges the operator's cancellation + damage policy text (Settings → Customer policies) into the SignWell agreement via template fields (api_ids `cancellation_policy`, `damage_policy`), gated behind **`SIGNWELL_POLICY_FIELDS=false`** (default off) so sends never break on a template lacking the fields. **Blocked on a real contract in SignWell** — the current template is the `basic-rental-agreement` placeholder. Activation, once a real rental contract exists: (1) add two non-required, non-signer **Text fields** to the template with API IDs exactly `cancellation_policy` and `damage_policy`; (2) set `SIGNWELL_POLICY_FIELDS=true` in Vercel + redeploy (**fields first, then the flag**, or the create call fails). Policies already surface at storefront checkout + in the quote/confirmation emails today — this only adds the contract itself. Pairs naturally with the single-signer template edit above (do both in one template pass).
- [~] **Connect remaining stubbed UI** — cleanup pass done. **Dashboard:** "Open route" now links to `/deliveries`; the weather **Draft heads-up** / **Dismiss** stubs removed (advisory kept as info); "New booking" now opens the Booking builder (see the operator-created quote/booking item). **Inquiries:** the **filter pills** (All / Needs you / Auto) now filter the list; the inert `SlidersHorizontal` button and decorative composer chips (Change quote / Offer date / Attach photo) removed; the header **Call / Text** replaced with a working **Email** (`mailto:`) action. **Calendar:** "New booking" left intentionally gated. ✅ dashboard **search** now jumps to the CRM: the header box (`CustomerSearchBox`) navigates to `/customers?q=…`, which seeds the Customers list's search. _Remaining: it searches **customers** (name/email/phone); a combined **bookings** search (by item/date/id) is a later enhancement._

- [~] **Promos & discounts** — **phases 1–2 shipped.** ✅ **Discount codes** (migration `0036`, commit `ece1f03`): a `promos` table + `/promos` manager (% or $ off, expiry window, min subtotal, usage limit, active toggle, redemption counts). Codes reduce the **items subtotal pre-tax** (one per booking, no stacking); `priceBreakdown` gains a `discount` that threads through storefront checkout, the operator booking builder, and the authoritative `createBooking` snapshot (`promo_id`/`promo_code`/`discount_cents`). Redemptions count when a booking is **paid** (webhook) or **recorded** (manual). Shows on checkout, pay page, booking detail, quote/confirmation emails; dashboard Month has a **"Discounts given"** tile. ✅ **Automatic promos** (migration `0037`, commit `1425750`): unified into the promos table via a `trigger` (code/weekday/repeat) + `weekdays` — **weekday** (rental day matches) and **repeat-customer** (prior committed booking) auto-apply with no code. A single `resolveBookingDiscount` picks the **best single discount** across a typed code + applicable auto-promos; typed codes can't redeem an auto-promo's name (`applyPromo` gated to `trigger='code'`). **⬜ Phase 3 — packages / bundles (LATER):** curated multi-item bundles at a set price ("bounce house + cotton candy = $220"). This is a **catalog-modeling** change, not a discount-engine one — a package is a catalog entry composed of component items, and booking it must **reserve each component's inventory** (touches the item model + `reserve_booking` / availability, and pricing where the bundle price overrides the sum of components). Independent of the discount work above. _Scope notes for whoever picks it up: decide package-vs-item modeling (a `packages` table with `package_items`, vs a special item `kind` with a components list); how availability aggregates (a package is available iff every component is); storefront display (one card, expandable components); and whether components can still be booked à la carte. Per-item **code scoping** (a code that only applies to specific items) is also still open from phase 1 — currently codes apply to the whole subtotal._

- [~] **Operator document management** — a private per-operator library for the business paperwork a rental operation keeps on hand. Operator-scoped + login-gated (sensitive records). **Phase A shipped** (migration `0040`):
  - ✅ **Document library** — a **private** `operator-docs` storage bucket + a `documents` table (type / label / expiry / optional booking+customer links). Upload (PDF / image / Word, 20 MB), label, download via **short-lived signed URLs** (never public — verified the public URL 400s), edit, delete; `/documents` page + nav entry.
  - ✅ **Document types** — COI, business license, safety/inspection, W-9, permit, waiver template, contract, other.
  - ✅ **Expiry tracking** — per-doc expiry date + a **dashboard warning** and page banner as insurance/licenses/permits near lapse (tracked types only).
  - ✅ **Attach to bookings / customers** — a DocumentsPanel on the booking detail + customer profile (upload with the record preset, list, download, remove); ownership-checked so a doc can't cross tenants.
  - ⬜ **Customer-facing exposure (later)** — publish a COI/safety cert on the storefront or attach to emails.
  - ⬜ **Expiry email reminders (later)** — email nudge reusing notification-prefs (dashboard warning is done).

- [~] **Per-operator contract identity & custom templates** — closed the multi-tenancy gap where every operator's customers signed a rental agreement branded to the **platform** (`SIGNWELL_SENDER_*`), not the operator. All under the platform's single SignWell account — **operators never need their own SignWell account**; per-operator SignWell accounts were deliberately rejected (no clean connect flow; platform bears the per-signature cost).
  - ✅ **Operator as counterparty** (migration `0039`): `sendAgreementForOrder` resolves the operator from the booking and makes them the named counterparty/countersigner (`business_address`, `esign_signer_name/email`, editable in Settings → Rental agreement), falling back to the platform identity only when unresolved. Safe default, no template change needed.
  - ✅ **B1 — per-operator template id** (migration `0041`): `operators.signwell_template_id`; the send path uses the operator's own SignWell template when set, else the platform default. Settings field to paste one.
  - ⚠️ **B2 — self-serve embedded editor** (commit `32ecb99`, code done, **BLOCKED on SignWell plan** — REVISIT): on a PDF in the doc library, **"Use as rental agreement"** → server creates a **draft** SignWell template from the file (`POST /document_templates`, `draft` + `api_application_id`, pre-seeded `Client`/`Document Sender` placeholders) → operator places signer fields in SignWell's **embedded editor** (`static.signwell.com/assets/embedded.js`) → on `completed` we persist the `template_id`. Gated behind `SIGNWELL_API_APPLICATION_ID`. **BLOCKER (2026-07-13):** the SignWell **pay-as-you-go API plan does NOT allow template *creation* via API** — `POST /document_templates` returns **401 `not_authorized_error`** (it worked earlier only under a full trial). Verified live: document creation (test + live) + **from-template document creation both work 201** — only *creating templates* is gated (a higher SignWell tier). So **B2 is unusable on the current plan**; the button 401s if clicked. _To revisit: (a) confirm which SignWell plan includes API template management + upgrade, then re-test `createDraftTemplate`; (b) meanwhile hide/disable the B2 button so it doesn't error (or handle the 401 with a "needs a template-capable plan" message); (c) then finish the manual editor click-through + confirm a completed template is `ready`._
  - ⬜ **Template-body merge fields + single-signer** — still behind flags (`SIGNWELL_TEMPLATE_FIELDS`, `SIGNWELL_SINGLE_SIGNER`); prints `company_*` + policy fields in the doc body / drops the countersigner once the SignWell **template is edited** (the single-signer + policy-embed items below).

  _Setup done: `SIGNWELL_API_APPLICATION_ID` added in Vercel + local. The single-signer + policy-embed items below are the remaining SignWell **template edits**._

## Tier 2 — Reliability & scale

- [ ] **Performance (infra) follow-ups** — _(app-level nav perf: cached session/operator resolution, parallelized queries, loading skeletons; **now also**: the **redundant per-nav RSC `auth.getUser()` is eliminated** — the middleware forwards the verified user id via a trusted, inbound-stripped header (`x-operator-user-id`) and Server Components read that (falling back to `getUser` when absent); **session→operator is a single join query** instead of two round-trips; and the middleware **skips `getUser` entirely on public routes** (storefront/APIs). ~430ms less session overhead per operator navigation.)_ Remaining (infra, needs setup): (a) **co-locate Supabase + Vercel in the same region** — each DB query is ~130ms; cross-region latency adds up. (b) **local JWT verification** (`getClaims` w/ asymmetric signing keys) to also drop the ~300ms `auth.getUser()` the **middleware** still makes on every operator request — requires enabling asymmetric JWT keys in the Supabase dashboard first.
- [x] **Overbooking race hardening** — the reserve step (booking → `pending_payment` at checkout) is now atomic. A Postgres `reserve_booking(booking_id)` function (migration `0020`) locks the booking's item rows (`FOR UPDATE`, ordered by id to avoid deadlocks), re-checks **peak** capacity over the rental range excluding this booking, then flips the status — so two simultaneous checkouts for the last unit serialize and the loser raises `OVERSELL`. `/api/checkout` calls it **before** creating the Stripe session and returns a clean 409 "just sold out" (surfaced in the storefront checkout). _Setup: run migration `0020` — **required before deploy** (checkout calls the RPC). Verified with a concurrent-reservation test. Note: the post-paid oversell guard in `markBookingPaid` stays as a logging backstop._
- [~] **Error monitoring** (Sentry) — ✅ **`@sentry/nextjs` wired across server/edge/client** via `src/instrumentation.ts` + `withSentryConfig` + a root `global-error.tsx` boundary. **Dormant until a DSN is set** (guarded on `NEXT_PUBLIC_SENTRY_DSN`/`SENTRY_DSN` → complete no-op, same pattern as our other integrations); source-map upload only runs when `SENTRY_AUTH_TOKEN` exists, so the build stays clean/offline. **Manual capture** added to the Stripe webhook's best-effort catches (balance email, booking emails, agreement send, top-level handler) — money-path failures that were previously console-only/invisible. Build + typecheck verified. _Remaining: structured logging; and the go-live step below._
  - [ ] **Sentry — go live (your action):** create a Sentry project (Next.js), then set in Vercel Production: **`NEXT_PUBLIC_SENTRY_DSN`** (the DSN — this alone turns on runtime capture). Optional for source maps / readable stack traces: **`SENTRY_AUTH_TOKEN`** + **`SENTRY_ORG`** + **`SENTRY_PROJECT`** (build-time only). Nothing else to change — the code is deployed and waiting on the DSN.
- [~] **Automated tests + CI** — ✅ **Vitest suite (79 tests)** covering the pure money/availability/access logic a silent bug would corrupt: `priceBreakdown` (tax/discount/rounding), delivery-fee resolution (flat/zones/distance + haversine), the bookable-date engine (`normalizeSchedule`/`isDateBookable`/`assessRange`), plan entitlement (`effectivePlanId` — the paid↔free downgrade belt), promo discount math, deposit, phone E.164, bookable units, roles. ✅ **GitHub Actions CI** (`.github/workflows/ci.yml`) gates every push/PR to `main` on **lint → typecheck → test** (no secrets needed). Scripts: `npm test` / `test:watch` / `test:coverage` / `typecheck`. _Remaining: DB-backed logic still needs a Supabase-mock harness (`applyPromo`/`resolveBookingDiscount`, availability arithmetic, webhook handlers); a **staging environment**; wire `build` into CI (needs dummy env). Config: `vitest.config.mts` (node env, `@/` via `vite-tsconfig-paths`)._
- [ ] **Accessibility pass** + mobile QA.

## Tier 3 — Growth & go-to-market

- [ ] **Marketing site / front of site (operator acquisition)** — the public top-of-funnel that sells Bounce to prospective **operators** (our SaaS customers), separate from the per-operator rental storefronts. Today the apex serves the app/login; there's no landing page pitching the product itself. Needs: a **marketing homepage** (hero + the AI-quote value prop — ideally a live/interactive taste of the storefront chat), a features section (AI quoting, calendar, inquiries inbox, Connect payouts, driver route), a **pricing page** wired to the existing plan selection → signup (Free / Solo $29 / Growing $59), social proof/testimonials, and supporting pages (about, contact, FAQ, plus the Tier-0 legal pages). Considerations: (1) **domain/route split** — marketing at the apex (`bounce-app.com`), operator storefronts at `/s/[slug]` (or subdomains), the operator app behind login; decide the split + redirects; (2) **SEO + perf** — static/ISR, page metadata, OG images, sitemap/robots; (3) **funnel** — every CTA lands on signup → the existing onboarding wizard; (4) **content** — start hardcoded, add a lightweight CMS later for a blog/SEO. _This is a **launch/GTM need**, not a nice-to-have — there's no operator-acquisition surface without it._
- [x] **Operator CRM — unified customer record** — a `customers` table (migration `0023`; per-operator, deduped by email→phone) that unifies what were disconnected name/email/phone copies across bookings + inquiries. **Customers list** (`/customers` — search + per-customer total spent / booking count / upcoming / last activity) and **profile** (`/customers/[id]` — contact with one-tap **Email / Call / Book again**, four stats (**Collected** net of refunds, Total booked, Bookings, Upcoming), editable **private notes**, and a timeline of their bookings + conversations). Each booking shows its **real payment history** — the `orders` transactions (deposit / balance / full / refund, with amount + status + date) and a **Balance-due** flag when a card payment is partial — so "Collected" is true net cash, distinct from booked value. **Cash counts too:** "Mark paid (cash)" and a manual booking's recorded cash deposit now write a `cash`-provider order (migration `0025`, `recordCashPayment`), so cash shows in the history (tagged "cash") and in Collected alongside Stripe. (`getOrderByBookingId` excludes cash so refund/detail still target the card order.) Kept in sync by an `upsertCustomer` at every contact-entry point (booking, inquiry, storefront email capture, SMS bootstrap); existing data backfilled. Bookings + inquiries carry a real **`customer_id` FK** (migration `0024`) set on write and backfilled — stats + the activity timeline join on it (no more email/phone matching, and the email-only-vs-phone-only dedup edge case is gone). This also clears the dashboard **"search customers"** gap and is the foundation for customer self-service + repeat-booking. _Setup: migrations `0023` + `0024` **required before deploy** — the `/customers` pages query the table and booking/inquiry inserts write `customer_id`._
- [ ] **Customer self-service (end-customer login)** — the customer-facing side of the CRM (split from the operator CRM above): a passwordless login for the person renting to see their booking history + upcoming, **re-book in a tap**, prefill checkout, track status, and pull receipts + the signed agreement. Layer a **platform-level identity** (a customer may rent from several operators) over the per-operator `customers` records, claiming existing orders by verified email/phone. Keep checkout **guest-first** — an account is a convenience, never a barrier.
- [~] **Operator API & embeddable storefront (bring-your-own-website)** — let operators run their inventory + AI quote agent + checkout on **their own site** instead of the hosted storefront. **Growing-plan-gated** (`PLAN_CAPABILITIES.apiAccess`). All under the platform's single infra — **operators never need their own SignWell/Stripe/etc.** The business logic was already operator-parameterized (pricing, availability, `handleInquiry`, `createBooking`, Stripe checkout); this is the **access layer** on top. Verified end-to-end against a running server at each phase.
  - ✅ **Phase 1 — API-key foundation** (migration `0042` + `0043`, commits `eb8c432`/`f359c75`): an `api_keys` table (publishable `pk_` / secret `sk_`, SHA-256 hashed — **publishable kept retrievable** Stripe-style so the snippet is always copyable, secret hash-only). `withApiKey` wrapper resolves the operator **from the key** (never a request body param — the core isolation guarantee), enforces key type + plan + CORS + `OPTIONS` preflight + per-key rate limiting. Publishable keys are **origin-restricted**. Two versioned public endpoints prove it: `GET /api/v1/catalog` + `POST /api/v1/agent` (agent forces the operator from the key + enforces the AI-quote cap), sharing logic with the storefront via `buildStorefrontCatalog` (one source of truth; `/api/items` refactored to it, output unchanged). **Settings → Developers** panel: create (reveal-once) / list / revoke keys, manage allowed origins, upsell + upgrade when off-plan.
  - ✅ **Phase 2 — embeddable widget** (commit `f359c75`): a copy-paste `<script src="…/embed.js" data-key="pk_live_…">` that iframes the storefront onto the operator's site — reusing the storefront verbatim in an `embed` mode (drops the app nav, forces the chat storefront, auto-sizes via `postMessage`, and hands Stripe checkout to the top page since hosted checkout can't render in an iframe). `/embed` resolves the key → operator; `/embed.js` is the loader; the snippet lives in Settings → Developers.
  - ✅ **Hardening** (commit `0e1a212`): **`frame-ancestors` CSP** — middleware sets it on `/embed` from the key's `allowed_origins` (edge-safe: Web Crypto + REST lookup) so a storefront only frames on registered domains (unknown/revoked key → `'none'`). **Checkout returns to the operator's site** — `embed.js` passes the host URL, `/embed` validates its origin against the allow-list (open-redirect safe), Stripe returns there with a `?bounce_checkout` marker, and the storefront shows a success/cancel confirmation banner.
  - ⬜ **Phase 3 — formal headless REST** — round out `/api/v1` (contact, booking, checkout-session, delivery/promo preview) reusing `withApiKey` + extracted handlers, a stable error contract, OpenAPI docs, and a **test mode** (`pk_test_`/`sk_test_`).
  - ⬜ **Phase 4 — outbound webhooks** — signed (HMAC) per-operator event delivery (`inquiry.created`, `booking.created`, `booking.paid`, `agreement.signed`) with retries, so an integrator's backend can react.
  - _Setup done: `SIGNWELL_*` unrelated; no new env for the widget. Migrations `0042` + `0043` applied. Follow-ups: subdomains/custom domains (Multi-tenancy); a native inline (non-iframe) widget only if operators need deep visual integration._

- [ ] **External CRM sync (integration)** — for operators who already live in HubSpot / Salesforce / GoHighLevel / Mailchimp etc.: push `customers` + bookings out via a native connector and/or a webhook + CSV export (and optionally Zapier), so their marketing/automation stack stays fed. Start **one-way** (our data → their CRM; the native CRM stays the source of truth), add two-way later. Best gated to the **Growing** plan as a paid integration. _Deliberately a follow-on to the native CRM above — not a replacement for it._
- [ ] **Other growth** — reviews & ratings, analytics dashboard, transactional SMS notifications (booking/reminder texts — distinct from the two-way inquiry SMS). _(Promo/discount codes + auto-promos are **shipped** — see "Promos & discounts" in Tier 1; multi-item packages/bundles is **phase 3** there.)_

---

## Recommended sequence

1. **Auth + session-scoped operator resolution** (keystone; schema is already tenant-ready). **← now**
2. **Stripe Connect** (onboarding needs it).
3. ~~**Operator onboarding wizard** + **inventory management UI**~~ ✅ (`/onboarding` checklist: geocoded location → add rentals → connect Stripe; new signups land here).
4. **Transactional email** + **balance collection** + **delivery fees/tax**.
5. **Prod DB split** + migration tooling (go-live checklist). ~~durable rate limit~~ ✅ Upstash · ~~AI-quote cap~~ ✅ migration `0038` · legal pages ✅ (drafts — counsel review + placeholders pending).
6. **Overbooking hardening** ✅ · **tests/CI** ✅ (Vitest 79 tests + GitHub Actions) · **Sentry** ✅ wired (dormant until DSN).
