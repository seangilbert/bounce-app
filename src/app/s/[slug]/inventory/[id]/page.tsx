export const dynamic = "force-dynamic";

/** Item detail view — rendered by the storefront shell (in the parent layout),
 *  which reads the item id from the pathname. This route is just a marker. */
export default function StoreItemDetailPage() {
  return null;
}
